<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" type="text/css" href="/assets/Index.scss">
  <style>
    table {
      width: 100%;
      border-top: 1px solid #444444;
      border-collapse: collapse;
    }
    th, td {
      border-bottom: 1px solid #444444;
      padding: 10px;
      text-align: center;
    }
  </style>
</head>

<div class="IndexName"><h1>Tables</h1></div>

<table>
  <thead>
  <tr>

    <th>Table_N</th>
    <th>N_seats</th>
    <th>Seated</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @tables.each do |table| %>
    <tr>
      <td><%= table.table_num %></td>
      <td><%= table.nseats %></td>
      <td><%= table.be_seated %></td>
      <td><%= link_to image_tag('/calendar.svg', size: 25), "/restaurant_table_management/index_schedule/#{@restaurant.id}/#{table.id}" %></td>
      <td><%= link_to image_tag('/waiting-room.svg', size: 25),"/restaurant_table_management/seated/#{table.id}",method: 'patch' %></td>
      <td><%= link_to image_tag('/chair.svg', size: 25),"/restaurant_table_management/out/#{table.id}",method: 'patch' %></td>

  <% end %>
  </tbody>
</table>

<br>




<%= link_to 'New table', "/restaurant_table_management/new_table/#{@restaurant.id}" %><br>
<%= link_to 'Back', "/restaurant_table_management/index_restaurant/" %>