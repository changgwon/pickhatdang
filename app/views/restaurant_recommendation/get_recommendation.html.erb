<!DOCTYPE html>
<html>
<head>

  <link rel="stylesheet" type="text/css" href="/assets/Index.scss">
  <style>
    table {
      width: 100%;
      border-top: 1px solid #444444;
      border-collapse: collapse;
    }
    th, td {
      border-bottom: 1px solid #444444;
      padding: 10px;
      text-align: center;
    }
  </style>
</head>

<div class="IndexName"><h1>추천 목록</h1></div>


<table>
  <thead>
  <tr>
    <th>Name</th>
    <th>Location</th>
    <th>Genre</th>
    <th>Price</th>
    <th>Rating</th>
    <th>Waiting</th>
    <th>Score</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @sorted_r.each do |r| %>
  <% recommend_r=Restaurant.find(r.r_id) %>
    <tr>
      <td><%= recommend_r.name %></td>
      <td><%= recommend_r.location %></td>
      <td><%= recommend_r.genre %></td>
      <td><%= recommend_r.pricerange %></td>
      <td><%= recommend_r.rating %></td>
      <td><%= recommend_r.waiting %></td>
      <td><%= r.score.round(2) %></td>
      <td><%= link_to image_tag('/takeaway.svg'), "/restaurant_recommendation/add_to_shopping_bag/#{r.r_id}" %></td>
  <% end %>
  </tbody>
</table>